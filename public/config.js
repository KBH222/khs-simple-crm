// Set this to your Railway app base URL to make the local UI use the Railway API.
// Example:
//   window.API_BASE_URL = 'https://your-railway-app.up.railway.app';
// Leave it as an empty string to use the same-origin API (default behavior).
window.API_BASE_URL = '';